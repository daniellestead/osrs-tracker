[package]
name = "reminder-widget"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.27"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dirs = "1.0"
chrono = { version = "0.4", features = ["serde"] }